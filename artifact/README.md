# Artifact Policy

This directory may hold sanitized reports, synthetic benchmark summaries, and
other review artifacts that are safe to commit. Do not store raw market data,
raw book text, embedding indexes, account state, live order payloads, wallet
addresses, secrets, or local runtime state here.

