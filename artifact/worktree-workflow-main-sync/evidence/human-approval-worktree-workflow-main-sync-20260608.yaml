schema_version: "0.1"
record_type: human_approval_evidence
status: recorded

identity:
  project_id: worktree-workflow-main-sync
  approval_id: human-approval-worktree-workflow-main-sync-20260608
  created_at: "2026-06-08"

scope:
  approved_action: approved_spec_freeze_and_subagent_implementation
  condition: "Proceed only if specification review is approved_for_human_review."
  source_ref: "user request in active thread: '仕様書を作成してレビューし、OKならサブエージェントに実行とレビューさせる'"
  satisfied_by: artifact/worktree-workflow-main-sync/evidence/spec-review-worktree-workflow-main-sync-20260608.yaml

limits:
  allowed:
    - "Freeze the reviewed specification."
    - "Create lane mapping and work contracts."
    - "Delegate implementation and review to scoped subagents."
  not_approved:
    - "Direct push to main or master."
    - "Merge."
    - "Branch deletion."
    - "Worktree deletion."
    - "Release or deployment."
    - "CI/CD or GitHub Actions changes unless later explicitly approved."
    - "Dependency changes."
    - "Secrets, auth, database, infrastructure, or protected data changes."

decision:
  status: approved
  next_action: approved_spec_freeze
