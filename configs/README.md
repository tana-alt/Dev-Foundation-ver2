# Configs

Tracked configs are templates and safe defaults only. Put secrets, exchange
credentials, account identifiers, raw data paths, and live runtime overrides in
local ignored files outside repo truth.

