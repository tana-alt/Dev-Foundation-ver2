{
  "_comment": "Codex hooks. Merge into ~/.codex/hooks.json (or inline [hooks] tables in config.toml). Same harness scripts as Claude -- the PostToolUse payload (tool_name/tool_input/tool_response) is shared, so one translator serves both. Stop emits {\"decision\":\"block\",\"reason\"} on a failing spec-gated gate.",
  "PostToolUse": [
    {
      "matcher": "*",
      "hooks": [
        {
          "type": "command",
          "command": "python3 scripts/hook_post_tool_use.py",
          "timeout": 30
        }
      ]
    }
  ],
  "Stop": [
    {
      "hooks": [
        {
          "type": "command",
          "command": "python3 scripts/hook_stop.py",
          "timeout": 600
        }
      ]
    }
  ]
}
