{
  "_comment": "Copy into .claude/settings.json to activate the harness as a Claude Code observe-mode runtime. SessionStart resurfaces open eval issues; PostToolUse records the trajectory; Stop runs the plan-gated completion loop. Do NOT activate in this repo's own .claude/ unless you want your own session gated.",
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 scripts/hook_session_start.py",
            "timeout": 30
          }
        ]
      }
    ],
    "PostToolUse": [
      {
        "matcher": "*",
        "hooks": [
          {
            "type": "command",
            "command": "python3 scripts/hook_post_tool_use.py",
            "timeout": 30
          }
        ]
      }
    ],
    "Stop": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "python3 scripts/hook_stop.py",
            "timeout": 900
          }
        ]
      }
    ]
  }
}
