schema_version: "0.1"
record_type: residual_risk_carryover_record
status: draft

identity:
  project_id: example_project
  work_id: example_work_id
  created_at: "YYYY-MM-DDTHH:mm:ssZ"

risks:
  - id: RISK-001
    severity: high
    type: human_gate_pending
    summary: "Human final review remains required."
    affected_requirement_ids: []
    affected_contract_ids: []
    source_refs:
      - "artifact/<project_id>/output/final-handoffs/<work_id>.yaml"
    current_evidence_refs: []
    owner_lane: ""
    human_decision_required: true
    decision_needed: "Human approval before protected action."
    accepted_by: not_accepted
    review_focus:
      - "Confirm protected action remains human-only."
    next_flow_seed:
      problem: "Complete human final review."
      source_refs:
        - "artifact/<project_id>/output/final-handoffs/<work_id>.yaml"

handoff:
  recommended_next_action: human_review_required
  final_handoff_ref: "artifact/<project_id>/output/final-handoffs/<work_id>.yaml"
